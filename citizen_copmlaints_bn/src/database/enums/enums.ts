export enum ComplaintStatus {
  SUBMITTED = "Submitted",
  IN_PROGRESS = "In Progress",
  RESOLVED = "Resolved",
  CLOSED = "Closed",
}

export enum UserType {
  CITIZEN="Citizen",
  AGENT = "Agent",
  ADMIN = "Admin",
}